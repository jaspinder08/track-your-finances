# Schemas Module (`schemas/`)

## Purpose
The `schemas/` directory contains Pydantic models used for request validation, response serialization, and type declarations across API endpoints.

## Contents
- **`health.py`**: Pydantic schema for health check responses (`HealthCheck`).
- **`__init__.py`**: Re-exports schemas for convenient imports across routers.
